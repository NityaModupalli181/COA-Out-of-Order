# CECS 530 — Speculative Decoding & Multi-Token Pipelines
# Python dependencies
# Install: pip install -r requirements.txt
#
# NOTE: PyTorch with CUDA must be installed separately:
# pip install torch==2.1.2+cu121 --index-url https://download.pytorch.org/whl/cu121

transformers==4.44.0
accelerate>=0.27.0
numpy>=1.24.0
matplotlib>=3.7.0
pytest>=7.4.0
